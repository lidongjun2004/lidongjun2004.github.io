---
title: "文远知行（挂）"
description: "AI Infra · 一面"
date: 2026-08-23
tags: ["实习", "AI Infra"]
---

## 一面（2026.1.6）

1. 盘项目

2. 八股：*全没回答上来，给面试官都问笑了。*

   - 讲讲 `C++11`

   - `C++` 中 `vector` 的原理

   - `vector.push_back()` 是 `O(1)` 的吗？原理是什么？

   - `python` 中 `==` 和 `is` 的区别是什么？

   - 讲讲 `start()` 和 `end()`

   - 重载运算符是怎么做的

   - 线程和进程的区别是什么？

   - `TCP` 协议是什么？

3. 算法题：

   - 给定一棵二叉树，求链的最大权重和。*说了一下思路秒了。*

   - 给定一颗二叉树，任意两父子不可同时取，求链的最大权重和。*写了一个贼丑陋的，但反正是对了。*
