---
title: "MiniMax（过）"
description: "AI Infra · 一面、二面"
date: 2026-08-23
tags: ["实习", "AI Infra"]
---

## 一面（2026.1.6）

1. 盘项目

2. 问有没有 AI Infra 经验。*没有。*

3. 算法题：给定一个文件路径的列表，将列表渲染成文件树的格式。*python 写的，过了。*

    ```text
    输入：
    [
        "/root/path_a/1.txt",
        "/root/path_b/3.txt",
        "/root1/4.txt",
        "/root/path_a/2.txt",
        "/root1/dir1"
    ]

    输出：
    - root
        - path_a
            - 1.txt
            - 2.txt
        - path_b
            - 3.txt
    - root1
        - 4.txt
        - dir1
    ```

## 二面（2026.1.21）

1. 盘项目

2. 八股：

   - 给定一个无向连通图，每条边含有边权，代价为割掉边的边权和，请给出代价最小的能把图分割为至少两个连通分支的方案，最好的时间复杂度是多少？*没学过，不会。查了一下是最小割算法。*

   - 都说 `C++` 很快，原理是什么？为什么它比 `python` 快？*不知道。*

   - 0 - 1 背包是 NP-Hard 问题，同时又有线性解法，请问这是为什么？*不知道。*

3. 算法题：给定俩个有序数组 `a` 和 `b`，求两个数组合并后第 `k` 小的元素。*二分一下，O(log k) 解法。*
