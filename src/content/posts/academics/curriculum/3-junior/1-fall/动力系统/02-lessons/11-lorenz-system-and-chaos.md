---
title: "洛伦兹系统与连续时间混沌"
description: "不动点、稳定性、叉式和 Hopf 分岔、耗散性与蝴蝶吸引子"
date: 2026-08-24
---

洛伦兹系统是三维自治连续系统产生混沌的经典例子：

$$
\begin{cases}
\dot x=\sigma(y-x),\\
\dot y=rx-y-xz,\\
\dot z=xy-\beta z,
\end{cases}
\qquad \sigma,r,\beta>0.
$$

在原始对流模型里，$x$ 表示对流强度，$y$ 与水平温差有关，$z$ 表示垂直温度分布偏离。

## 不动点

令三式为零。第一式给 $y=x$，第三式给 $z=x^2/\beta$。代入第二式：

$$
x\left(r-1-\frac{x^2}{\beta}\right)=0.
$$

所以原点

$$
P_0=(0,0,0)
$$

始终存在；当 $r>1$ 时还有

$$
P_\pm=
\left(\pm\sqrt{\beta(r-1)},
\pm\sqrt{\beta(r-1)},r-1\right).
$$

## 原点稳定性与叉式分岔

雅可比矩阵

$$
J=
\begin{pmatrix}
-\sigma&\sigma&0\\
r-z&-1&-x\\
y&x&-\beta
\end{pmatrix}.
$$

在原点，$-\beta$ 是一个特征值，另外两个满足

$$
\lambda^2+(\sigma+1)\lambda+\sigma(1-r)=0.
$$

- $0<r<1$：所有特征值实部为负，原点稳定；
- $r>1$：常数项为负，出现正特征值，原点成为鞍点。

在 $r=1$，两个对称不动点从原点产生，构成叉式分岔。

## 非零不动点与 Hopf 边界

在 $P_\pm$ 处，特征多项式为

$$
\lambda^3+a_1\lambda^2+a_2\lambda+a_3=0,
$$

其中

$$
a_1=\sigma+\beta+1,\quad
a_2=\beta(\sigma+r),\quad
a_3=2\sigma\beta(r-1).
$$

Routh–Hurwitz 条件要求 $a_1,a_2,a_3>0$ 且 $a_1a_2>a_3$。边界给出

$$
r_H=\frac{\sigma(\sigma+\beta+3)}{\sigma-\beta-1},
$$

前提是 $\sigma>\beta+1$。经典参数

$$
\sigma=10,\qquad \beta=\frac83
$$

给出 $r_H\approx24.74$。

## 对称性

变换

$$
(x,y,z)\mapsto(-x,-y,z)
$$

保持方程不变。因而 $P_+$、$P_-$ 成对出现，轨道若在一个“翅膀”附近绕行，也有一个镜像轨道在另一个翅膀附近。

## 耗散性

向量场散度是常数：

$$
\nabla\cdot\boldsymbol f=-\sigma-1-\beta<0.
$$

随流运动的小体积满足

$$
V(t)=V(0)e^{-(\sigma+1+\beta)t}.
$$

所以三维体积被持续压缩。耗散并不意味着轨道收敛到一个点；拉伸与折叠可在整体收缩中形成分形吸引子。

## 为什么会有“蝴蝶”

轨道在某个非零不动点附近旋转并被逐渐甩出，随后穿过中央区域，落到另一个对称区域，再重复类似过程。每次停留时间对初值极敏感，产生看似随机的左右切换。

这种机制同时具备：

- 局部拉伸：邻近轨道分离；
- 全局折叠：轨道被带回有界区域；
- 体积收缩：长期集合维数低于三维。

## 混沌的三个常用特征

1. 对初值敏感，最大 Lyapunov 指数为正；
2. 长期有界但不收敛到周期轨道；
3. 吸引子具有分形几何结构。

数值图“看起来乱”不是严格判据。最大 Lyapunov 指数必须用变分方程和周期归一化等可靠算法估计。

## 经典参数的时间线要谨慎

对 $\sigma=10,\beta=8/3$：

- $r<1$：原点稳定；
- $r=1$：叉式分岔；
- $1<r<r_H$：两个非零平衡点局部稳定，但某些参数段可有复杂暂态；
- $r=r_H$：非零平衡点经亚临界 Hopf 失稳；
- $r=28$：经典混沌吸引子。

不要把整个 $1<r<24.74$ 简单写成“周期”或“暂态混沌”，实际全局结构更细。

## 考试要点

- 三个不动点必须会从方程直接推导。
- 叉式临界值固定为 $r=1$；Hopf 临界值依赖 $\sigma,\beta$。
- 散度负证明体积收缩，不单独证明混沌。
- 对称性解释两翼，但不保证每条轨道按固定节奏切换。
