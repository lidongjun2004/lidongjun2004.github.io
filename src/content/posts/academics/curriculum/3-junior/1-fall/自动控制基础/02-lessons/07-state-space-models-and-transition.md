---
title: "状态空间模型与状态转移"
description: "从状态变量和动态方程出发，推导矩阵指数、状态方程解、传递函数矩阵与坐标变换"
date: 2026-08-23
---

传递函数只描述零初始条件下的输入输出关系；状态空间把系统内部“此刻保存了哪些足以决定未来的信息”也纳入模型，更适合多输入多输出、非零初态和现代控制设计。

## 1. 什么是状态

若已知时刻 $t_0$ 的一组最少变量 $x(t_0)$，以及 $t\ge t_0$ 的输入 $u(t)$，就能唯一确定系统之后的行为，那么这些变量构成状态变量。

状态变量不是唯一的。只要两组变量之间由可逆变换联系，它们都能完整描述同一系统。

## 2. 线性状态空间模型

线性时不变系统写成

$$
\dot x(t)=Ax(t)+Bu(t),
$$

$$
y(t)=Cx(t)+Du(t).
$$

其中：

- $x\in\mathbb R^n$ 是状态向量；
- $u\in\mathbb R^m$ 是输入；
- $y\in\mathbb R^p$ 是输出；
- $A$ 决定内部自由演化；
- $B$ 描述输入如何作用于状态；
- $C$ 描述怎样从状态形成输出；
- $D$ 是输入到输出的直接通道。

SISO 课件常把 $B,C$ 写成 $b,c$。

## 3. 怎样选状态变量

对 $n$ 阶微分方程，常选

$$
x_1=y,\quad x_2=\dot y,\quad\ldots,\quad x_n=y^{(n-1)}.
$$

这样前 $n-1$ 个状态方程只是“下一项等于导数”，最后一项由原微分方程给出。

对结构图，还可把每个积分器输出选作状态。原因是积分器保存历史信息，知道所有积分器当前值和未来输入就能继续演化。

选状态时要做到足够且最少。重复变量或可由其他状态代数决定的变量不应额外加入。

## 4. 齐次状态方程与矩阵指数

无输入时

$$
\dot x=Ax,
$$

解为

$$
x(t)=e^{A(t-t_0)}x(t_0).
$$

状态转移矩阵定义为

$$
\Phi(t)=e^{At}
=I+At+\frac{A^2t^2}{2!}+\cdots.
$$

它把初始状态沿系统自由动力学从一个时刻传播到另一个时刻。

## 5. 状态转移矩阵的性质

对 LTI 系统：

$$
\Phi(0)=I,
$$

$$
\Phi(t_1+t_2)=\Phi(t_1)\Phi(t_2),
$$

$$
\Phi^{-1}(t)=\Phi(-t),
$$

$$
\frac{d}{dt}\Phi(t)=A\Phi(t)=\Phi(t)A.
$$

普通标量指数的加法性质能保留，是因为这里前后都是同一个矩阵 $A$。对不交换的不同矩阵，一般没有 $e^{(A+B)t}=e^{At}e^{Bt}$。

## 6. 如何计算 $e^{At}$

### 拉普拉斯反变换

$$
\mathcal L\{e^{At}\}=(sI-A)^{-1},
$$

因此

$$
e^{At}=\mathcal L^{-1}\{(sI-A)^{-1}\}.
$$

低阶矩阵时可直接求逆、部分分式展开。

### 对角化

若 $A=P\Lambda P^{-1}$，则

$$
e^{At}=Pe^{\Lambda t}P^{-1},
$$

而 $e^{\Lambda t}$ 只需对每个特征值取指数。

### Jordan 形

Jordan 块 $J=\lambda I+N$ 中 $N$ 为幂零矩阵：

$$
e^{Jt}=e^{\lambda t}
\left(I+Nt+\frac{N^2t^2}{2!}+\cdots\right).
$$

重根的 $te^{\lambda t}$ 等项正来自幂零部分。

还可以用 Cayley–Hamilton 定理把高次 $A$ 化成 $I,A,\ldots,A^{n-1}$ 的线性组合。

## 7. 非齐次状态方程的解

有输入时，解为

$$
x(t)=e^{A(t-t_0)}x(t_0)
+\int_{t_0}^{t}e^{A(t-\tau)}Bu(\tau)\,d\tau.
$$

第一项是初始状态引起的零输入响应；第二项是输入经系统卷积形成的零状态响应。

若 $u(t)=u_0$ 为常量且 $A$ 可逆，可化为

$$
x(t)=e^{At}x(0)+A^{-1}(e^{At}-I)Bu_0.
$$

但 $A$ 不可逆时仍应使用积分式，不能强行求 $A^{-1}$。

## 8. 从状态空间到传递函数矩阵

零初始条件下做拉普拉斯变换：

$$
sX(s)=AX(s)+BU(s),
$$

$$
X(s)=(sI-A)^{-1}BU(s).
$$

代入输出方程：

$$
G(s)=\frac{Y(s)}{U(s)}
=C(sI-A)^{-1}B+D.
$$

MIMO 时 $G(s)$ 是传递函数矩阵，第 $(i,j)$ 项描述第 $j$ 个输入到第 $i$ 个输出的通道。

## 9. 坐标变换

令旧状态与新状态满足

$$
x=Pz,
$$

$P$ 可逆，则

$$
\dot z=P^{-1}APz+P^{-1}Bu,
$$

$$
y=CPz+Du.
$$

新模型为

$$
\bar A=P^{-1}AP,\quad
\bar B=P^{-1}B,\quad
\bar C=CP,\quad
\bar D=D.
$$

坐标变了，输入输出传递函数不变：

$$
\bar C(sI-\bar A)^{-1}\bar B+D
=C(sI-A)^{-1}B+D.
$$

这叫代数等价。特征值、可控性和可观测性也不会因可逆坐标变换而改变。

## 10. 传递函数与状态空间的边界

同一传递函数可以有不同阶数、不同内部模式的状态实现。只有既可控又可观的实现才是最小实现；被零极点对消的内部模式不会出现在输入输出传递函数中，却可能影响内部稳定性。

因此“传递函数稳定”代表 BIBO 稳定，不一定保证任意内部初态都衰减。后续可控性和可观测性正是连接内部结构与外部行为的桥梁。
