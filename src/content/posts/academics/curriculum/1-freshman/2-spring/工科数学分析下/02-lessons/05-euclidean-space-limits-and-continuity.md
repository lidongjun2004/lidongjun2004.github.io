---
title: "第 5 讲：Euclid 空间、多元极限与连续"
description: "从邻域、开闭集和紧致性出发，理解多元函数极限为何必须同时控制所有趋近路径。"
date: 2026-08-27
---

多元分析不是把 $x$ 换成 $(x,y)$ 就结束了。最大的变化是：一点可以从无穷多个方向、无穷多条曲线趋近，因此一元里“左右极限相等”的检查方式不再够用。

## $\mathbb R^n$ 中的距离与邻域

对 $\mathbf x=(x_1,\dots,x_n)$、$\mathbf y=(y_1,\dots,y_n)$，Euclid 距离是

$$
\|\mathbf x-\mathbf y\|
=\sqrt{\sum_{i=1}^{n}(x_i-y_i)^2}.
$$

点 $\mathbf a$ 的 $\delta$ 邻域为

$$
B_\delta(\mathbf a)
=\{\mathbf x:\|\mathbf x-\mathbf a\|<\delta\}.
$$

在二维它是开圆盘，在三维是开球。多元极限中的“足够靠近”就是落入某个小球，而不是只沿坐标轴靠近。

### 内点、边界点和聚点

- 若某个小球完全包含在集合 $D$ 中，$\mathbf a$ 是 $D$ 的内点；
- 若每个小球既碰到 $D$ 又碰到其补集，$\mathbf a$ 是边界点；
- 若每个去心小球都含有 $D$ 中其他点，$\mathbf a$ 是聚点。

所有点都是内点的集合称为开集；包含全部极限点的集合称为闭集。一个集合可以既开又闭，例如整个 $\mathbb R^n$ 和空集。

有界闭集在 $\mathbb R^n$ 中是紧集。它保留了一元闭区间上的几个核心结论：连续函数在紧集上有界、能取到最大最小值，并且一致连续。

## 多元极限的定义

设 $\mathbf a$ 是定义域的聚点。若

$$
\forall\varepsilon>0, \exists\delta>0,
\quad0<\|\mathbf x-\mathbf a\|<\delta
\Rightarrow |f(\mathbf x)-A|<\varepsilon,
$$

就称 $f(\mathbf x)\to A$。

这个定义要求**所有**足够靠近的点都满足误差要求。于是：

- 沿任意一条路径得到同一个极限，是极限存在的必要条件；
- 找到两条路径得到不同极限，足以证明极限不存在；
- 检查若干条常见路径结果相同，仍不能证明极限存在。

例如

$$
f(x,y)=\frac{xy}{x^2+y^2}.
$$

沿 $y=x$ 趋近原点得到 $1/2$，沿 $y=-x$ 得到 $-1/2$，所以极限不存在。

更隐蔽的例子是某些函数沿每条直线都趋于同一值，却沿抛物线给出别的值。因此想证明存在，应使用统一估计，而不是枚举路径。

## 证明极限存在的三种办法

### 直接夹逼

把绝对值压成只依赖 $r=\sqrt{x^2+y^2}$ 的量。例如

$$
\left|\frac{x^2y}{x^2+y^2}\right|
\le |y|
\le\sqrt{x^2+y^2}\to0.
$$

这个估计对所有路径同时成立，所以能证明极限。

### 极坐标

令 $x=r\cos\theta,y=r\sin\theta$。如果能得到

$$
|f(r,\theta)-A|\le g(r),\qquad g(r)\to0,
$$

且上界与 $\theta$ 无关，就证明了极限。若结果仍依赖 $\theta$，通常提示极限不存在；但不能仅因表达式含 $\theta$ 就下结论，还要看依赖是否随 $r\to0$ 消失。

### 利用连续复合

多项式处处连续；分母不为零处的有理函数连续；初等连续函数的复合仍连续。因此在连续点可直接代入，无须重新做 $\varepsilon$-$\delta$ 证明。

## 多元连续

$f$ 在 $\mathbf a$ 连续的条件仍是

$$
\lim_{\mathbf x\to\mathbf a}f(\mathbf x)=f(\mathbf a).
$$

对分段定义函数，常见任务是选择原点处的值使其连续。先证明去心部分的极限存在，再把函数值设为该极限；若不同路径极限不同，就不存在任何可补的值。

连续性和偏导数是两件事。函数可能在原点各偏导都存在，却不连续；也可能连续但不可微。后续判断可微时，必须回到“统一线性近似”的定义。

## 多重极限与累次极限

二重极限

$$
\lim_{(x,y)\to(a,b)}f(x,y)
$$

要求 $x,y$ 同时任意趋近。累次极限

$$
\lim_{x\to a}\lim_{y\to b}f(x,y)
$$

则规定了先后顺序。二重极限存在通常能约束累次极限，但两个累次极限都存在且相等，并不自动保证二重极限存在。

做题时先看符号：若题目写的是二重极限，就不能只算两种先后次序；若写累次极限，就必须按给定顺序处理。
