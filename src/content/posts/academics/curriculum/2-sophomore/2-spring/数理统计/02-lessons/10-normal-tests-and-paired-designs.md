---
title: "第 10 讲：正态总体检验与配对设计"
description: "系统整理正态均值、方差、两均值差和配对样本的检验统计量与前提。"
date: 2026-08-27
---

正态总体检验的公式很多，但选择逻辑很机械：先看检验的是均值还是方差，再看一个总体还是两个总体，最后看方差是否已知、两样本是否独立。

## 单总体均值

检验 $H_0:\mu=\mu_0$。

方差已知：

$$
Z=\frac{\bar X-\mu_0}{\sigma/\sqrt n}\sim N(0,1).
$$

方差未知：

$$
T=\frac{\bar X-\mu_0}{S/\sqrt n}\sim t(n-1).
$$

双侧拒绝域分别是 $|Z|>z_{1-\alpha/2}$ 或 $|T|>t_{1-\alpha/2}(n-1)$。

## 单总体方差

正态性在这里尤其重要。检验 $H_0:\sigma^2=\sigma_0^2$ 使用

$$
Q=\frac{(n-1)S^2}{\sigma_0^2}\sim\chi^2(n-1).
$$

双侧拒绝域是

$$
Q<\chi^2_{\alpha/2}(n-1)
\quad\text{或}\quad
Q>\chi^2_{1-\alpha/2}(n-1).
$$

$\chi^2$ 分布不对称，不能用“绝对值大于某值”表达双侧拒绝域。

## 两独立总体均值差

检验 $H_0:\mu_1-\mu_2=\Delta_0$。

方差已知时用正态统计量。方差未知但相等时：

$$
T=\frac{(\bar X-\bar Y)-\Delta_0}
{S_p\sqrt{1/n_1+1/n_2}}
\sim t(n_1+n_2-2).
$$

方差不等时使用 Welch $t$ 统计量与近似自由度。回答时必须说明采用了哪种方差假设。

## 两正态方差

检验 $H_0:\sigma_1^2=\sigma_2^2$ 时，

$$
F=\frac{S_1^2}{S_2^2}\sim F(n_1-1,n_2-1).
$$

双侧时两端都要保留。若为了查表把较大的样本方差放在分子，就要相应调整尾概率，不能仍机械使用原自由度顺序。

## 配对检验

同一对象的前后测量记为 $(X_i,Y_i)$。定义 $D_i=X_i-Y_i$ 后，检验

$$
H_0:\mu_D=0
$$

使用

$$
T=\frac{\bar D}{S_D/\sqrt n}\sim t(n-1).
$$

配对检验利用了对内相关性；相关越强，差值方差越小，检验功效通常越高。前提是配对之间独立，而不是每一对内部独立。

## 模型前提与稳健性

- 均值的 $t$ 检验对轻度非正态较稳健，尤其样本较大时；
- 方差的 $\chi^2/F$ 检验对非正态很敏感；
- 异常值会同时影响均值与方差；
- 有明显偏态或离群点时，应先画图，再考虑变换或非参数检验。

答案最后应落回实际问题，例如“在 $\alpha=0.05$ 水平下，数据提供了两种工艺平均强度不同的证据”，不要只停在“拒绝 $H_0$”。
