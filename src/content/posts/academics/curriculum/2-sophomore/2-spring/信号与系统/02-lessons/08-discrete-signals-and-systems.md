---
title: "第 8 讲：离散时间信号与系统"
description: "理解序列表示、基本运算、差分方程，以及离散系统的线性、时不变、因果和稳定性。"
date: 2026-08-27
---

离散时间信号是按整数序号排列的序列。$n$ 可以来自连续信号的等间隔采样，也可以本来就是事件编号。不要把 $x[n]$ 理解成“在整数之外等于零的连续函数”：它在非整数处根本未定义。

## 表示方法

序列可用公式、数列、表格或若干移位单位样值的和表示：

$$
x[n]=\sum_{k=-\infty}^{\infty}
x[k]\delta[n-k].
$$

常见序列：

$$
a^n u[n],\quad
\cos(\Omega_0n+\phi),\quad
u[n],\quad\delta[n].
$$

离散复指数 $e^{j\Omega n}$ 对频率是 $2\pi$ 周期的：

$$
e^{j(\Omega+2\pi k)n}=e^{j\Omega n}.
$$

因此离散频率只需研究任意长度 $2\pi$ 的区间。

## 时间运算

- 移位：$x[n-n_0]$ 右移 $n_0$；
- 反转：$x[-n]$；
- 抽取：$x[Mn]$ 保留每隔 $M$ 个样本；
- 插值式扩展：$x[n/L]$ 仅在 $n$ 为 $L$ 的倍数时取原值，其余位置需定义为 0。

离散尺度变换不能连续拉伸图形，因为索引必须仍为整数。

## 离散系统表示

系统映射 $y[n]=\mathcal T\{x[n]\}$。线性常系数差分方程为

$$
\sum_{k=0}^{N}a_ky[n-k]
=\sum_{m=0}^{M}b_mx[n-m].
$$

给定输入和足够的初始条件，可以递推输出。方程只规定输入输出关系；还需初始静止等附加条件才能唯一确定系统。

## 系统性质

线性和时不变的检验与连续系统相同，只把 $t_0$ 换成整数移位 $n_0$。

因果系统的 $y[n_0]$ 只依赖 $x[k]$（$k\le n_0$）。若表达式含 $x[n+1]$，通常非因果。

无记忆系统的 $y[n]$ 只依赖同一时刻 $x[n]$。例如 $y[n]=n x[n]$ 无记忆、线性但时变；$y[n]=x^2[n]$ 无记忆、时不变但非线性。

## 差分方程的自然响应

齐次方程

$$
\sum_{k=0}^{N}a_ky[n-k]=0
$$

试解 $y[n]=\lambda^n$，得到特征多项式。不同根产生 $C_i\lambda_i^n$，$r$ 重根产生

$$
(C_0+C_1n+\cdots+C_{r-1}n^{r-1})\lambda^n.
$$

这与连续微分方程的指数模式完全平行，只是 $e^{\lambda t}$ 换成 $\lambda^n$。

## 零输入与零状态

$$
y[n]=y_{\mathrm{zi}}[n]+y_{\mathrm{zs}}[n].
$$

零输入响应由初始状态产生，按齐次方程演化；零状态响应由输入产生，可用卷积或 z 变换求。

离散系统的“初始条件”常指递推开始前若干输出值。做单边 z 变换时，这些值会出现在移位公式中。

## LTI 系统由单位样值响应决定

单位样值响应 $h[n]=\mathcal T\{\delta[n]\}$。利用单位样值展开、线性和时不变性：

$$
y[n]=\sum_{k=-\infty}^{\infty}
x[k]h[n-k].
$$

于是

- 因果 $\Longleftrightarrow h[n]=0$（$n<0$）；
- BIBO 稳定 $\Longleftrightarrow \sum_n|h[n]|<\infty$；
- 无记忆 $\Longleftrightarrow h[n]=K\delta[n]$。
